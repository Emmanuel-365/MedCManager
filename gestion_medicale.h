#ifndef GESTION_MEDICALE_H
#define GESTION_MEDICALE_H

// Inclusions des structures de données
#include "patient.h"
#include "medicament.h"
#include "personnel.h"
#include "consultation.h"

// Définitions globales si nécessaire
// Par exemple, tailles maximales pour les listes en mémoire (si statiques)
// #define MAX_PATIENTS 100
// #define MAX_MEDICAMENTS 200
// etc.
// Pour l'instant, nous allons viser une gestion dynamique, donc pas de limites ici.

// Prototypes des fonctions globales de gestion (seront définies dans les .c correspondants)
// Cette section sera étoffée au fur et à mesure de l'implémentation des fonctionnalités.

// Fonctions utilitaires (si besoin, ex: pour la gestion des dates, saisies sécurisées)
// void afficher_date(Date d);
// void saisir_date(Date* d);

#endif //GESTION_MEDICALE_H
